server:
  port: 8082 # This sets the application server port, not the database port

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/apidemo
    username: root
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
